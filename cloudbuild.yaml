steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: 'bash'
  args: ['-c', 'gcloud config set app/cloud_build_timeout 1600 && gcloud app deploy myserviceone.yaml && gcloud app deploy myservicetwo.yaml']
timeout: '1600s'
